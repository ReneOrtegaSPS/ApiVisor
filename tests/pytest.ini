[pytest]
markers =
    common_errors: Tests for common errors across multiple endpoints
    new_tests: Testing new tests
    normal_flow: Tests for a normal flow using the API
    big_files_flow: Tests for files greater than 6mb